version: '3.9'

services:
  db:
    container_name: postgres_mbc
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DATABASE}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  builder: 
    build: 
      context: ./app
      dockerfile: ./Dockerfile
      target: builder 
      args:
        DATABASE_URL: ${DATABASE_URL}
    depends_on:
      - db
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NODE_ENV: development
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - ./.env
    working_dir: /app
    command: yarn dev
    stdin_open: true
    tty: true
  app-dev:
    image: node:24.5-slim
    working_dir: /app
    volumes:
      - ./app:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=${NODE_ENV}


volumes:
  postgres_data: