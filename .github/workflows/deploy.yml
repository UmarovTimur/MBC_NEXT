name: Build and Deploy to FTP

on:
  push:
    branches:
      - main
      - az
  workflow_dispatch:
    inputs:
      deploy_target:
        description: 'Deploy to test server'
        required: true
        default: 'az'
        type: choice
        options:
          - az
      branch:
        description: 'Branch to deploy'
        required: true
        default: 'develop'
        type: string

jobs:
  deploy-uz:
    name: Build & Deploy uz (static)
    if: github.ref_name == 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.5.0"

      - name: Enable Corepack
        run: corepack enable

      - name: Create .env file
        working-directory: ./apps/uz
        run: echo "${{ secrets.ENV_FILE }}" > .env

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn workspace @mbc/uz build
        env:
          NODE_ENV: production

      - name: Upload to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./apps/uz/out/
          server-dir: /

  deploy-az-test:
    name: Deploy az + admin → test server
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.TEST_SSH_HOST }}
          username: ${{ secrets.TEST_SSH_USER }}
          key: ${{ secrets.TEST_SSH_KEY }}
          port: ${{ secrets.TEST_SSH_PORT }}
          script: |
            cd ${{ secrets.TEST_APP_PATH }}
            git fetch origin
            git checkout ${{ github.event.inputs.branch }}
            git pull origin ${{ github.event.inputs.branch }}
            corepack enable
            yarn install --frozen-lockfile
            echo "${{ secrets.ENV_FILE_AZ_TEST }}" > apps/az/.env
            echo "${{ secrets.ENV_FILE_ADMIN_TEST }}" > apps/admin/.env
            yarn workspace @mbc/admin build
            pm2 restart admin --update-env
            pm2 restart az --update-env

  deploy-az:
    name: Build & Deploy az (static)
    if: github.ref_name == 'az'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.5.0"

      - name: Enable Corepack
        run: corepack enable

      - name: Create .env file
        working-directory: ./apps/az
        run: echo "${{ secrets.ENV_FILE_AZ }}" > .env

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn workspace @mbc/az build
        env:
          NODE_ENV: production

      - name: Upload to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER_AZ }}
          username: ${{ secrets.FTP_USERNAME_AZ }}
          password: ${{ secrets.FTP_PASSWORD_AZ }}
          local-dir: ./apps/az/out/
          server-dir: /
